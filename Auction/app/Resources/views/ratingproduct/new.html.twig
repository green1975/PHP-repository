{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ratingproduct creation</h1>
    {{ form_start(form) }}
     {{ 5|rating }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('ratingproduct_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
